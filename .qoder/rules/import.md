---
trigger: always_on
---

# Flutter 插件使用规范

## 复用优先原则

在生成涉及第三方插件或组件的代码时，**必须优先检查当前项目中是否已有可复用的实例**，禁止重复引入功能相同的插件。

## 检查流程

1. **查阅 `pubspec.yaml`**：确认项目已依赖的插件列表

- 若确实需要新增插件，需说明原因，并确认与现有插件无功能重叠
- 所有新增插件需兼容项目当前的 Flutter SDK 版本
- 插件版本需与 `pubspec.yaml` 中的约束保持一致
